BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Report] ADD [WWKNESDB] DECIMAL(32,16),
[WWKNSO] DECIMAL(32,16),
[lastEditor] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
