BEGIN TRY

BEGIN TRAN;

-- CreateTable
CREATE TABLE [dbo].[TempTabulation] (
    [ID] NVARCHAR(15) NOT NULL,
    [STO] NVARCHAR(1000),
    [TR] INT,
    CONSTRAINT [TempTabulation_ID_key] UNIQUE NONCLUSTERED ([ID])
);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
